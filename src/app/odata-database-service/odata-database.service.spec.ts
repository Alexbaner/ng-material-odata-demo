import { TestBed, inject } from '@angular/core/testing';

import { ODataDatabaseService } from './odata-database.service';

describe('ODataDatabaseService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [ODataDatabaseService]
    });
  });


});
